main class Application.java


Unit tests

Integration tests (RestApiTest class)

Smoke tests (smoke-test.sh)


User stories

1) Create a Offer{ID, Description, Currency, Price, ValidityInDays} POST

2) Get by name GET

3) Show all valid offers (not cancelled or expired)

3) Edit by name PUT

4) Cancel by id DELETE

5) Query by Description (not expired nor cancelled only)

6) Simple HATEOAS (links)